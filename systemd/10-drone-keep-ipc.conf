# Drop-in cho systemd-logind. Cài: xem README.md mục "Tự chạy lúc boot".
#
# Stack ROS 2 chạy bằng drone-startup.service dưới user pc và truyền dữ liệu giữa các node qua bộ
# nhớ chia sẻ (Fast DDS SHM, /dev/shm/fastrtps_*). RemoveIPC=yes (mặc định Ubuntu) XOÁ bộ nhớ đó mỗi
# khi phiên đăng nhập cuối của pc kết thúc - tức mỗi lần một phiên SSH đóng. Các node mất liên lạc
# giữa chừng, EKF nhận dữ liệu rác và trôi nhiều mét (gặp thật 2026-09-18).
[Login]
RemoveIPC=no
